<UserControl x:Class="WorkspacesModule.Condition.SubconditionAffixesControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkspacesModule.Condition"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="{Binding AffixTypeName}"/>

        <ContentControl Grid.Row="0" Grid.Column="2" Margin="0, 5" Content="{Binding FirstAffix}"/>
        <ContentControl Grid.Row="1" Grid.Column="2" Margin="0, 5" Content="{Binding SecondAffix}"/>
        <ContentControl Grid.Row="2" Grid.Column="2" Margin="0, 5" Content="{Binding ThirdAffix}"/>
    </Grid>
</UserControl>
