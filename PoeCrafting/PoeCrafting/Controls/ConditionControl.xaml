<UserControl x:Class="PoeCrafting.UI.Controls.ConditionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PoeCrafting.UI.Controls"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" MinHeight="200"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200"/>
            <ColumnDefinition Width="60" />
        </Grid.ColumnDefinitions>

        <ComboBox Grid.Column="0" Grid.Row="0" SelectedItem="{Binding SelectedSubcondition}" ItemsSource="{Binding SubconditionControls}" DisplayMemberPath="SubconditionName"></ComboBox>
        <Button Grid.Column="1" Grid.Row="0" Content="Add" Click="OnAddClick"/>
        <ContentControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="{Binding SelectedSubcondition}"/>
    </Grid>
</UserControl>
