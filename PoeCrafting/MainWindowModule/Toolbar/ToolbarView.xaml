<UserControl x:Class="MainWindowModule.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainWindowModule"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="_Open" Command="{Binding Path=OpenCommand}" />
            <MenuItem Header="_Save" Command="{Binding Path=SaveCommand}" />
            <MenuItem Header="_Exit" Command="{Binding Path=CloseCommand}" />
        </MenuItem>
    </Menu>
</UserControl>
